{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Zig: Run",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/a.out",
        }
    ]
}